import { FormEvent,useState } from 'react'; import { supabase } from '../lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [pass,setPass]=useState('');const [signup,setSignup]=useState(false);const [msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();setMsg('');const r=signup?await supabase.auth.signUp({email,password:pass}):await supabase.auth.signInWithPassword({email,password:pass});if(r.error)setMsg(r.error.message);else if(signup)setMsg('Conta criada. Verifique o e-mail, se solicitado.')}async function google(){const {error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:location.origin}});if(error)setMsg(error.message)}return <main className="login"><section className="card"><h1>📚 Figurinhas</h1><p>Controle seus álbuns e figurinhas.</p><button className="google" onClick={google}>Continuar com Google</button><div className="sep">ou</div><form onSubmit={submit}><input placeholder="E-mail" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input placeholder="Palavra-passe" type="password" value={pass} onChange={e=>setPass(e.target.value)} required minLength={6}/><button type="submit">{signup?'Criar conta':'Entrar'}</button></form><button className="link" onClick={()=>setSignup(!signup)}>{signup?'Já tenho conta':'Criar uma conta'}</button>{msg&&<p className="msg">{msg}</p>}</section></main>}
